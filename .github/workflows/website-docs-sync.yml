name: Sync website docs

on:
  push:
    branches: [main]
    paths:
      - docs/**
      - docs.json
      - docs.zh.json
      - docs-site/**
      - scripts/sync-docs-site.mjs
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Mint website dispatch token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.INTERPRETERWORK_APP_ID }}
          private-key: ${{ secrets.INTERPRETERWORK_APP_PRIVATE_KEY }}
          owner: openinterpreter
          repositories: website
      - name: Dispatch website sync
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          gh api repos/openinterpreter/website/dispatches --method POST --field event_type=open_interpreter_docs_changed
